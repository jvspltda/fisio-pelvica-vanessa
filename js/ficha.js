/* js/ficha.js — estrutura de dados da ficha feminina. Preenchido em F3. */
(function (raiz) {
  'use strict';
  var Ficha = {};
  raiz.Ficha = Ficha;
  if (typeof module !== "undefined") module.exports = Ficha;
})(typeof window !== "undefined" ? window : globalThis);
